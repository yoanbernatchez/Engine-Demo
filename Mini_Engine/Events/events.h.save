#ifndef EVENTS_H_INCLUDED
#define EVENTS_H_INCLUDED

#include <SDL.h>
#include <stdbool.h>
#include "../Engine/globals.h"

/**
 * @brief Transforms SDL_Events to booleans stored in an EngineEvent structure.
 *
 * @param sdlEvent: SDL_Event structure.
 * @param event: Pointer to the EngineEvent we store data into.
 *
 * @return True if we keep handling events, false if we stop handling events.
 */
bool eventHandleEvents(SDL_Event sdlEvent, EngineEvent* event);

#endif /* KEYBOARD_EVENTS_H_INCLUDED */
