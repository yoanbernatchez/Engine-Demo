#ifndef EVENTS_H_INCLUDED
#define EVENTS_H_INCLUDED

#include <SDL.h>
#include <stdbool.h>
#include "../Engine/globals.h"

/**
 * @brief Transforms SDL_Events to booleans stored in an EngEvent structure.
 *
 * @param sdl_event: SDL_Event structure.
 * @param event:     Pointer to the EngEvent we store data into.
 *
 * @return True if we keep handling events, false if we stop handling events.
 */
bool eng_event_handle_events (SDL_Event sdl_event, EngEvent *event);

#endif /* KEYBOARD_EVENTS_H_INCLUDED */
